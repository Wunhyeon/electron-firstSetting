// CONSTANT
const apiUrl = 'https://api-origin.com/'

// LINK (renderer에서 쓰이는)
export const HOME_PAGES = '/'
export const MY_PAGE = '/my-page'

// Handler key (ipcHandler의 key)
export const HANDLE_HELLO = 'hello'

// API URL (외부 api 콜)
export const GET_HELLO = apiUrl + 'api/hello'
